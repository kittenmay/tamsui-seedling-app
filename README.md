# 淡水農友助手（基礎版）

以 **HTML + Tailwind CSS（CDN）** 建立的靜態首頁雛形，包含：

- 大標題：「淡水農友助手」
- 「今日淡水天氣預報」（假資料）
- 「本月推薦種植」（以 4 月為例）

## 啟動方式

在專案根目錄執行：

```bash
python -m http.server 8000
```

然後用瀏覽器打開 `http://localhost:8000/`。

